general {
  output_format ="i3bar"
  colors = true
}

order += "cpu_temperature 2"
order += "load"
order += "tztime local"
order += "tztime tokyo"

tztime local {
  format = "%Y-%m-%d %H:%M:%S"
}

tztime tokyo {
  format = "Tokyo: %H:%M:%S"
  timezone = "Asia/Tokyo"
}

cpu_temperature 2 {
  format = "%degrees °C"
  max_threshold = 75
}
